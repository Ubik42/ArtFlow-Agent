# M2-S1 typed projection and Scene Lab evidence

Date: 2026-08-25  
Evidence level: A2 — local API, browser and frozen Scene Package fixture  
GPU or paid provider used: no

## Outcome

- `/api/agent/runs` and `/api/agent/runs/{run_id}` expose a versioned compact projection built
  from the verified event chain and reducer state;
- the projection includes scene facts, status, budgets, pending decisions, a human-readable event
  timeline and bounded capability descriptions without raw database payloads, hash-chain internals,
  binaries or unbounded logs;
- unknown runs return 404 and a tampered event chain returns an explicit 409;
- the React workbench has been rebuilt as Scene Lab: a scene/constraint canvas, Agent Pulse event
  river, Evidence Lens and command deck replace the legacy dashboard composition;
- the real RTX 4080 run remains usable and is explicitly labelled legacy evidence rather than
  being presented as event-sourced Agent state;
- loading, empty, degraded, durable Agent and legacy evidence surfaces are intentional states;
- the UI uses no remote font, image or design dependency and retains reduced-motion behavior.

## Browser evidence

- `artifacts/goal/m2-s1-scene-lab-agent.png`: 1440×1000 durable Agent projection;
- `artifacts/goal/m2-s1-scene-lab-wide-loaded.png`: 1440×1000 real legacy baseline;
- `artifacts/goal/m2-s1-scene-lab-narrow.png`: 390×844 structural narrow layout.

The browser console reported 0 errors and 0 warnings. Reloading the durable fixture reconstructed
the same five-event timeline and budget status from SQLite.

## Verification

```text
python -m ruff check <M1/M2 Agent runtime, projection, API and focused tests>
All checks passed

python -m pytest <focused Agent, API, RunStore and Scene Package tests> -q
16 passed

npm run build
Vite production build passed

powershell -ExecutionPolicy Bypass -File scripts/validate.ps1 -Tier quick
42 passed in 1.21s
```

## Evidence ceiling

This proves a read-only event projection and a real browser-rendered Scene Lab foundation. Live SSE,
interactive durable approval, comparison overlays, recovery UX and a real Unreal-originated scene
remain pending.
