{
  "schemaVersion": "codex-goal-state@2.0.0",
  "stateRevision": 49,
  "strategyVersion": "4.0.0",
  "goalId": "artflow-agent",
  "objective": "Build an Unreal-native 2D visual-intent to verified 3D scene-change Agent whose durable coordinator plans typed scene deltas, orchestrates bounded image, material, asset, PCG and lighting capabilities, executes only in isolated staging, independently evaluates rerenders and 3D constraints, corrects failed domains and publishes replayable provenance-bound changes.",
  "status": "active",
  "currentMilestone": "M14",
  "milestones": [
    {
      "id": "M0",
      "title": "Real Comfy baseline and contract-first seams",
      "status": "completed",
      "dependsOn": [],
      "outcome": "The real RTX 4080 baseline remains reproducible and scene, provider, route, approval and receipt boundaries validate across Python, TypeScript and Unreal C++.",
      "acceptance": ["Real baseline and cross-language contract evidence are recorded without overstating human adoption"]
    },
    {
      "id": "M1",
      "title": "Hand-built Agent kernel and scene context",
      "status": "completed",
      "dependsOn": ["M0"],
      "outcome": "A verified Scene Package becomes durable AgentRun context in an append-only SQLite event model reconstructed by a deterministic reducer and exposed through a bounded capability registry.",
      "acceptance": [
        "Restart reconstructs the same state and pending interrupt from events",
        "Scene attachment and tool side effects are idempotent",
        "The model cannot mutate authoritative state or bypass typed policy"
      ]
    },
    {
      "id": "M2",
      "title": "Scene Lab and typed Agent event UI",
      "status": "completed",
      "dependsOn": ["M1"],
      "outcome": "A redesigned visual-production workspace exposes scene constraints, Agent lifecycle, tool actions, persisted approvals, recovery and evidence through typed streaming state.",
      "acceptance": ["Refresh preserves the current run and pending decision, and every visible proof maps to real state or evidence"]
    },
    {
      "id": "M3",
      "title": "Real Unreal scene and matched provider routing",
      "status": "completed",
      "dependsOn": ["M2"],
      "outcome": "One real Unreal scene package runs through local ComfyUI and Codex built-in GPT Image 2 using the same constraints and normalized receipts without a permission prompt.",
      "acceptance": ["Both real execution surfaces are bound to the same verified scene intent, persist normalized receipts, recover idempotently and remain explicitly unselected"]
    },
    {
      "id": "M4",
      "title": "Evaluation tribunal and bounded revision",
      "status": "completed",
      "dependsOn": ["M3"],
      "outcome": "Independent deterministic and multimodal evaluators expose disagreement, reject attractive-invalid candidates and verify one evidence-selected mask-bounded revision.",
      "acceptance": ["An attractive negative control is rejected and evaluator evidence replays without regenerating images"]
    },
    {
      "id": "M5",
      "title": "Recovery observability memory and evaluation",
      "status": "completed",
      "dependsOn": ["M4"],
      "outcome": "The Agent survives injected failures without duplicate side effects, emits correlated OpenTelemetry evidence, governs production memory and reports frozen Harness evaluations.",
      "acceptance": ["Recovery, context, policy and memory scorecards report denominators, latency and cost from reproducible cases"]
    },
    {
      "id": "M6",
      "title": "Verified provenance and portfolio case study",
      "status": "completed",
      "dependsOn": ["M5"],
      "outcome": "The complete Unreal-to-provider-to-tribunal-to-adoption-to-reimport story ships with independently verified C2PA provenance and inspectable evidence for every public claim.",
      "acceptance": ["One successful run and one failure-injected run can be independently replayed and verified"]
    },
    {
      "id": "M7",
      "title": "Scene Digital Twin and staged Scene Delta kernel",
      "status": "completed",
      "dependsOn": ["M6"],
      "outcome": "A real UE 5.8 scene exports stable 3D facts and accepts a frozen typed lighting/PCG SceneChangePlan whose dry-run proves dependency, scope, fingerprint, deterministic seed, validation and cleanup semantics before any host mutation.",
      "acceptance": [
        "The extended scene package records actor, material, light, PCG, data-layer, bounds and protected-object facts with content hashes",
        "A discriminated SceneChangePlan supports bounded lighting and PCG operations, rejects arbitrary code and validates as a dependency DAG",
        "A real UE dry-run receipt resolves staging strategy and write scope without modifying the source level"
      ]
    },
    {
      "id": "M8",
      "title": "ComfyUI production graph and PBR material route",
      "status": "completed",
      "dependsOn": ["M7"],
      "outcome": "Reviewed ComfyUI subgraphs and verified production nodes compile visual intent into validated PBR texture sets and Unreal material instances without arbitrary graph execution.",
      "acceptance": [
        "Runtime capability inspection proves every required custom node and workflow version",
        "One real PBR set passes channel, size, hash and material-instance validation in UE"
      ]
    },
    {
      "id": "M9",
      "title": "PCG lighting and asset scene-transformation closed loop",
      "status": "completed",
      "dependsOn": ["M8"],
      "outcome": "One project-owned concept target drives staged lighting, PCG layout and bounded asset/material changes, same-camera rerender evaluation, failed-domain correction and verified publish or discard.",
      "acceptance": [
        "The source camera, protected actors and walkable bounds remain invariant while the staged 3D scene visibly changes",
        "Technical and visual evaluators produce independent evidence and correction reruns only the failed branch",
        "The real UE result is published or discarded from a replayable receipt with no duplicate side effect"
      ]
    },
    {
      "id": "M10",
      "title": "MCP interoperability image-to-3D experiment and portfolio release",
      "status": "completed",
      "dependsOn": ["M9"],
      "outcome": "A thin MCP facade exposes the same bounded resources and tools, one optional image-to-3D provider passes through Interchange validation, and a Chinese screenshot-rich case study truthfully demonstrates the complete Unreal scene-transformation Agent.",
      "acceptance": [
        "MCP calls enter the existing policy, event, idempotency and validation path and expose no arbitrary host execution",
        "One generated GLB candidate is either admitted or rejected by recorded license, geometry, material, collision and budget checks while the main route still works without it",
        "The public release distinguishes implemented, experimental and planned capabilities and maps every claim to host evidence"
      ]
    },
    {
      "id": "M11",
      "title": "Live Scene Session and Scene Change Spectrum",
      "status": "completed",
      "dependsOn": ["M10"],
      "outcome": "A real imported Unreal scene opens an interactive Scene Session where the user states a visual intent, scopes bounded image, material, asset, PCG and lighting domains, and inspects a deterministic typed Scene Delta draft through ArtFlow's distinctive Scene Change Spectrum interface.",
      "acceptance": [
        "Scene Session planning derives only from the attached scene, registered capabilities and explicit user scope and causes no external side effect",
        "The interface communicates domain readiness, dependencies and guarded boundaries without resembling a chat shell, arbitrary node graph or generic AI dashboard",
        "A staged execution request can be content-bound to the draft and current scene without granting arbitrary host execution"
      ]
    },
    {
      "id": "M12",
      "title": "Unreal editor session bridge and candidate execution",
      "status": "completed",
      "dependsOn": ["M11"],
      "outcome": "The Unreal editor can initiate a Scene Session, receive a typed candidate-stage request and execute registered scene tools in an isolated candidate level with durable receipts and reconciliation.",
      "acceptance": [
        "One editor-originated session completes inspect, draft, stage, rerender and reconcile against the project-owned UE 5.8 host",
        "Retry and restart produce no duplicate asset, Actor or provider side effect"
      ]
    },
    {
      "id": "M13",
      "title": "Cross-pipeline scene transformation and failed-domain correction",
      "status": "completed",
      "dependsOn": ["M12"],
      "outcome": "Reviewed ComfyUI graphs, GPT Image 2 development orchestration, PBR materials, project and generated assets, PCG and lighting cooperate through one typed plan, with independent evaluation and failed-domain-only correction.",
      "acceptance": [
        "At least two materially different production cases use different capability routes while sharing the same coordinator and evidence model",
        "A deliberately failed domain is corrected without rerunning successful branches"
      ]
    },
    {
      "id": "M14",
      "title": "Embedded delivery and portfolio release",
      "status": "in_progress",
      "dependsOn": ["M13"],
      "outcome": "ArtFlow ships as a polished Chinese portfolio product with an Unreal-facing entry, multiple reproducible production cases, restrained documentation, real screenshots and a traceable release package.",
      "acceptance": [
        "A reviewer can understand, launch and verify the core workflow without reading development history",
        "Public claims and screenshots are generated from frozen host receipts and versioned cases"
      ]
    }
  ],
  "nextSlice": {
    "id": "M14-S1",
    "milestone": "M14",
    "title": "Reviewer-first launch path and traceable portfolio release",
    "outcome": "Turn the verified ArtFlow runtime into a reviewer-ready Chinese portfolio delivery: make the Unreal entry and local showcase launch path obvious, align README and demo guidance around the two current M13 cases, freeze a compact screenshot-rich walkthrough, and build a content-addressed release that can be independently verified without exposing development conversation, private state or generated runtime clutter.",
    "risk": "R2",
    "evidenceTarget": "A4",
    "requiresRealHosts": true,
    "allowedPaths": ["integrations/unreal/**", "src/artflow_agent/**", "tests/**", "web/**", "docs/**", "scripts/**", "config/goal-state.json", "artifacts/goal/**", "README.md", "pyproject.toml", "uv.lock"],
    "nonGoals": [
      "Do not publish, merge or overwrite the active/source Unreal level",
      "Do not rerun image, ComfyUI, asset import or PCG work whose frozen M13 receipts already satisfy the public case",
      "Do not add another Provider, framework, protocol, Agent role or case only to enlarge the feature list",
      "Do not redesign the whole frontend again; polish only issues that block reviewer understanding or target-size presentation",
      "Do not modify shared engine installation files or unrelated Unreal projects",
      "Do not delay delivery for packaged-runtime support, broad engine matrices, coverage targets or cosmetic test expansion"
    ],
    "acceptance": [
      "A reviewer can start the fixed local showcase and understand the Unreal-to-Agent-to-candidate loop, two capability routes and failed-domain correction from the first README screen and current Chinese UI",
      "The Unreal integration guide names the actual editor command, candidate-only mutation boundary and local Agent endpoint without requiring internal development knowledge",
      "Desktop and narrow screenshots cover the current two-case interface with zero console errors, blocking overlays or invented statuses",
      "The portfolio release manifest includes the current README, demo guide, key screenshots and evidence summaries, verifies content hashes independently, and excludes credentials, event databases, temporary candidates and conversation artifacts",
      "A clean clone can run the documented lightweight showcase path using repository-owned frozen evidence"
    ],
    "stopConditions": [
      "Stop before any source-level Unreal save or candidate adoption",
      "Stop if release assembly would include secrets, local databases, ignored runtime state or unreviewed generated binaries",
      "Stop if a public claim cannot cite a current frozen artifact, exact denominator or real host receipt",
      "Stop if visual polish requires replacing the current Scene Change Spectrum product language with a generic template",
      "Stop if launch documentation would depend on the user's login state or machine-private absolute paths",
      "Stop if project-owned UE host changes overlap unknown user edits and cannot be preserved"
    ],
    "environmentRequirements": [
      {"id": "node", "kind": "command", "value": "node", "required": true},
      {"id": "python", "kind": "command", "value": "python", "required": true},
      {"id": "unreal_editor", "kind": "command", "value": "C:/Program Files/Epic Games/UE_5.8/Engine/Binaries/Win64/UnrealEditor-Cmd.exe", "required": false}
    ],
    "validationCommands": [
      "python scripts/validate_goal_state.py",
      "powershell -ExecutionPolicy Bypass -File scripts/validate.ps1 -Tier quick"
    ]
  },
  "manualTracks": [],
  "continuationPolicy": {
    "sourceOfTruth": ["config/goal-state.json", "the checkpoint referenced by lastCheckpoint", "observable code, tests and current artifacts", "docs/AGENT_ENGINEERING_BLUEPRINT.md", "docs/development/CODEX_GOAL.md and docs/development/CODEX_LOOP.md"],
    "maxInProgressMilestones": 1,
    "checkpointRequiredBeforeAdvance": true,
    "executeValidationCommandsDynamically": false,
    "dirtyWorktreePolicy": "preserve_and_report",
    "manualTrackPolicy": "non_blocking_unless_promoted"
  },
  "lastCheckpoint": "artifacts/goal/checkpoint-0044.json",
  "currentBlocker": null,
  "evidenceCeiling": "M0-M13 are complete. M13-S1 binds a current RTX 4080 ComfyUI capability snapshot and real 23.094-second PBR execution to a validated five-channel texture set, Unreal material instance, project asset, PCG and lighting candidate. M13-S2 binds a real Codex GPT Image 2 visual target to the same Session coordinator, classifies an injected 0.05 / 6500K failure as lighting only, applies one lighting patch, preserves image/material/asset/PCG evidence hashes, and reconciles the corrected UE 5.8 candidate in a new process with 12 instances and zero repeated external submission. Both keep source ArtFlowDemo.umap SHA-256 620e4814... unchanged and are shown in the current Chinese desktop and narrow UI. This evidence does not yet claim a packaged release, clean-clone launch verification or source-level publish.",
  "constraints": [
    "ArtFlow Agent remains the independent flagship portfolio product; the sibling Art Pipeline Skill has separate Git and goal state and is only a reusable capability layer",
    "Project-owned DCC and engine fixtures, bounded local GPU runs, built-in Codex image generation, candidate selection and local portfolio delivery are autonomous development scope; unrelated user data and shared installations remain protected",
    "No arbitrary model-generated ComfyUI graph, Python, Shell or host-script execution",
    "No deterministic failure may be overridden by model confidence",
    "Candidate adoption is owned by the orchestrating Codex workflow and must cite persisted evaluation evidence rather than a permission prompt",
    "Use multiple Agent roles only for context isolation, distinct authority or real parallel work",
    "No framework, protocol, RAG or memory system is added without a measured requirement",
    "Tests focus on contracts, policy, replay, recovery and external boundaries and should stay near 15-25 percent of implementation effort",
    "Every public metric identifies its dataset, denominator and inspectable evidence",
    "Existing user work and historical source provenance must be preserved",
    "All scene mutations are staged in a project-owned Data Layer, Level Instance or candidate level before publish; the source level is not overwritten in place",
    "MCP is a thin adapter over the existing typed runtime and never owns a parallel state machine or arbitrary code-execution tool",
    "Image-to-3D providers produce candidates only; no generated mesh enters a scene before license, Interchange, geometry, material, collision and budget validation"
  ]
}
